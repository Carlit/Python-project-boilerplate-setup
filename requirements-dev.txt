# Dépendances de développement et de CI.
# Installation : pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.2.0,<9.0
pytest-cov>=5.0.0,<7.0
flake8>=7.1.0,<8.0

# Typage statique (mypy strict, cf. pyproject.toml [tool.mypy])
mypy>=1.11,<2.0
pandas-stubs>=2.2.0,<3.0

# Hooks locaux (flake8, mypy) exécutés avant chaque commit
pre-commit>=3.7,<4.0
