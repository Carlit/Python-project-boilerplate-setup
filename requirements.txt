# DÃ©pendances applicatives (runtime)
# Versions Ã©pinglÃ©es par borne mineure : compromis stabilitÃ© / correctifs de sÃ©curitÃ©.

SQLAlchemy>=2.0.30,<2.1
pandas>=2.2.0,<3.0
python-dotenv>=1.0.1,<2.0

# Driver PostgreSQL (psycopg 3, binaire : pas de compilation requise)
psycopg[binary]>=3.2.0,<4.0

# Driver Oracle (mode "thin" par dÃ©faut : aucun Instant Client Ã  installer)
oracledb>=3.3.0,<5.0
