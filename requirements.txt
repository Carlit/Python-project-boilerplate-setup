# Dépendances applicatives (runtime)
# Versions épinglées par borne mineure : compromis stabilité / correctifs de sécurité.

SQLAlchemy>=2.0.30,<2.1
pandas>=2.2.0,<3.0
python-dotenv>=1.0.1,<2.0

# Driver PostgreSQL (psycopg 3, binaire : pas de compilation requise)
psycopg[binary]>=3.2.0,<4.0

# Driver Oracle (mode "thin" par défaut : aucun Instant Client à installer)
oracledb>=3.3.0,<5.0

# Export Excel (src/export.py) : requis par pandas pour DataFrame.to_excel()
openpyxl>=3.1.2,<4.0
# Le format Parquet (src/export.py) necessite pyarrow, non installe par
# defaut : a ajouter separement si ce format est utilise.
